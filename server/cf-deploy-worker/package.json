{
  "name": "huaxushuo-cf-deploy-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker 中转：让浏览器直传 zip 到 Cloudflare Pages，避开浏览器 CORS + Direct Upload 多步脆弱性",
  "main": "src/worker.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.25.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
